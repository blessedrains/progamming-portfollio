void setup() {
	size(500,500);
	background(255);
}

void draw() {
	//goal: show the path of the ball as it moves, done 
	//background(255);
	//goal: randomize the color during runtime
	fill(random(255), random(255) ,random(255));
	//goal: allow the ellipse to move with the mouse, done 
	//goal randomize the size of the ball, done
    
	ellipse(mouseX, mouseY, random(50),random(50)); 
	ellipse 
}
